import { requireUser } from "@/lib/supabase/server";import { createAdminClient } from "@/lib/supabase/admin";import { fail,ok,safeError } from "@/lib/api";
export async function POST(_:Request,{params}:{params:Promise<{roomId:string}>}){try{const a=await requireUser();if(!a)return fail("UNAUTHORIZED","Sesi tidak ditemukan.",401);const {roomId}=await params;await createAdminClient().from("room_participants").update({left_at:new Date().toISOString(),connection_status:"disconnected",is_ready:false}).eq("room_id",roomId).eq("user_id",a.user.id);return ok({left:true});}catch(e){return safeError(e);}}
